using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DeskriceControl : LogicControl
{
    // Start is called before the first frame update
    void Start()
    {
        block.name = "ricedesk";
        block.type = "desk";
        block.content = "rice";
    }
    // Update is called once per frame
    void Update()
    {
        
    }
}
