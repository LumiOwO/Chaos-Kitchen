using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DesksubmitControl : LogicControl
{
    // Start is called before the first frame update
    void Start()
    {
        block.name = "submitdesk";
        block.type = "desk";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
