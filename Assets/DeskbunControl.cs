using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DeskbunControl : LogicControl
{
    // Start is called before the first frame update
    void Start()
    {
        block.name = "bundesk";
        block.type = "desk";
        block.content = "bun";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
