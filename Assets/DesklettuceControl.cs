using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DesklettuceControl : LogicControl
{
    // Start is called before the first frame update
    void Start()
    {
        block.name = "lettucedesk";
        block.type = "desk";
        block.content = "lettuce";
    }
    // Update is called once per frame
    void Update()
    {
        
    }
}
