using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class riceControl : LogicControl
{
    // Start is called before the first frame update
    void Start()
    {
        block.name = "rice";
        block.type = "food";
    }
    // Update is called once per frame
    void Update()
    {
        
    }
}
