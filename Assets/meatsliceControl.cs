using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class meatsliceControl : LogicControl
{
    // Start is called before the first frame update
    void Start()
    {
        block.name = "meatslice";
        block.type = "food";
    }


    // Update is called once per frame
    void Update()
    {
        
    }
}
